import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.blank.cap4',
  appName: 'cs-blank-cap4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
